# Contributing

1. Fork the repository and create a branch for your change.
2. Install dependencies with `npm install`.
3. Run `npm test` and `npm run lint`.
4. Keep changes focused and add or update tests when behavior changes.
5. Open a pull request with a clear summary of the problem and fix.
